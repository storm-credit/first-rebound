# Repository Operating Rules

## 1. Non-negotiable manuscript lock

`control/DESIGN_GATE.md`가 `OPEN`이 되기 전에는 다음을 만들거나 수정하지 않는다.

- 프롤로그, 본편, 에필로그 원고
- 장면 샘플, 1화 테스트 원고, 대사 중심 시험 장면
- `manuscripts/`, `episodes/*/draft*` 또는 원고로 오인될 수 있는 파일

설계 문서 안에서는 사건의 **기능과 결과**만 기술한다. 완성된 소설 문장으로 확장하지 않는다.

## 2. 권위 순서

충돌 시 다음 순서를 따른다.

1. 사용자의 최신 명시 결정
2. `canon/PROJECT_FREEZE.md`
3. 분야별 권위 문서(`canon/`, `research/`, `simulation/`, `design/`)
4. `PROJECT_STATE.md`
5. `context-packs/`의 파생 자료
6. 대화 기록과 작업 메모

하나의 사실에는 하나의 권위 문서만 둔다. Context Pack은 정본을 바꾸지 못한다.

## 3. 작업 방식

이 프로젝트는 다음 두 중앙 저장소를 참조한다.

- `minimum-action-agent-os`: 의도 확인, 맹점 훑기, 함정 점검, 4개 대안 비교, 최소 권한 작업, 상태 관리, 독립 검수
- `novel-writing-skills`: 결말/주제 → 하우스 스타일 → 서사 장치 → 참고작 기능 추출 → Act → Sub-Act → 통합 검수 → 회차 기능표 → Context Pack → 집필 → 견인력 검수

중앙 저장소의 내용을 복제해 또 다른 권위로 만들지 않는다. 작품별 결정과 결과만 이 저장소에 기록한다.

## 4. 결정 규칙

- 중요한 미결정 사항은 기본적으로 상호 배타적인 4개 시안을 만든 뒤 하나를 선택한다.
- 이미 확정된 질문을 다시 묻지 않는다.
- 조사되지 않은 실제 역사·규정·통계는 추정으로 잠그지 않고 `HOLD`로 둔다.
- 참고작은 기능을 추출하되 문장, 장면 배열, 관계, 대사, 고유 설정을 모사하지 않는다.
- 실존 인물은 사실 검증 대상이다. 허구 인물의 감정과 사생활을 실존 인물에게 임의로 부여하지 않는다.
- 변경 전 점검하고, 변경 후 검증하며, 되돌릴 수 있는 브랜치/PR 단위를 선호한다.

## 5. 설계 원칙

- Act마다 주된 설계 방법은 1~2개만 사용한다.
- Sub-Act마다 주 장치 1개와 필요할 때만 보조 장치 1개를 둔다.
- 사건은 `목표 → 압력 → 선택 → 비용 → 상태 변화`로 기록한다.
- 역사 고증은 장식이 아니라 인물의 선택지를 제한하는 규칙이다.
- “일어날 일은 일어난다”는 결과 고정이 아니라 원인 압력이 유지된다는 뜻이다.
- 주인공이 접촉한 경기·선수·팀·드래프트·트레이드는 원역사를 강제로 보존하지 않고 다시 계산한다.

## 6. 완료 정의

작업 완료 보고에는 반드시 다음이 있어야 한다.

- 실제 산출물 링크 또는 경로
- 수용 기준 충족 여부
- 남은 위험과 `HOLD`
- `PROJECT_STATE.md` 갱신
- 독립 검수 결과

문서가 존재한다는 것과 설계가 완료됐다는 것은 다르다.
